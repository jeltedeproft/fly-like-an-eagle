import{defaultProgress,UPGRADES,PARTS,MILESTONES,VEHICLES}from'./config.js';
const KEY='fly-like-an-eagle-v1';
export function sanitize(raw){const base=defaultProgress();if(!raw||typeof raw!=='object')return base;const levels={};for(const u of UPGRADES)levels[u.id]=Math.max(0,Math.min(u.max,Math.floor(Number(raw.levels?.[u.id])||0)));const parts={};for(const part of PARTS)parts[part.id]=raw.parts?.[part.id]===true;const knownMilestones=new Set(MILESTONES.map(m=>m.id)),knownVehicles=new Set(VEHICLES.map(v=>v.id)),claimedMilestones=[...new Set(Array.isArray(raw.claimedMilestones)?raw.claimedMilestones.filter(id=>knownMilestones.has(id)):[])],unlockedVehicles=[...new Set(['cart',...(Array.isArray(raw.unlockedVehicles)?raw.unlockedVehicles.filter(id=>knownVehicles.has(id)):[])])],selectedVehicle=unlockedVehicles.includes(raw.selectedVehicle)?raw.selectedVehicle:'cart';const volume=(value,fallback)=>Number.isFinite(Number(value))?Math.max(0,Math.min(1,Number(value))):fallback;return{...base,bolts:Math.max(0,Math.floor(Number(raw.bolts)||0)),best:Math.max(0,Number(raw.best)||0),runs:Math.max(0,Math.floor(Number(raw.runs)||0)),reducedMotion:Boolean(raw.reducedMotion),musicVolume:volume(raw.musicVolume,base.musicVolume),sfxVolume:volume(raw.sfxVolume,base.sfxVolume),levels,parts,claimedMilestones,unlockedVehicles,selectedVehicle};}
export function loadProgress(store=globalThis.localStorage){try{return sanitize(JSON.parse(store.getItem(KEY)))}catch{return defaultProgress()}}
export function saveProgress(progress,store=globalThis.localStorage){store.setItem(KEY,JSON.stringify(sanitize(progress)));}
