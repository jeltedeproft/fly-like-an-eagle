import{UPGRADES,PARTS,FACILITY_UPGRADES,POWER_SYSTEMS,upgradeCost,vehicleUpgradeCost,vehiclePartCost,powerCost}from'./config.js';
export function scoreRun({distance,landing,rewardScale=1}){const meters=Math.max(0,Number.isFinite(distance)?distance:0),base=Math.floor(meters*Math.max(1,rewardScale));const landingBonus=landing==='clean'?Math.max(10,Math.round(base*.2)):landing==='rough'?Math.max(5,Math.round(base*.03)):0;return{base,landingBonus,total:base+landingBonus};}
export function purchase(progress,id,vehicleId=progress.selectedVehicle||'cart'){const upgrade=UPGRADES.find(u=>u.id===id),build=progress.vehicleProgress?.[vehicleId];if(!upgrade||!build)return{ok:false,reason:'Unknown upgrade',progress};const level=build.levels[id]??0;if(level>=upgrade.max)return{ok:false,reason:'MAXED',progress};const cost=vehicleUpgradeCost(upgrade,level,vehicleId);if(progress.bolts<cost)return{ok:false,reason:`NEED ${cost-progress.bolts}`,progress};return{ok:true,cost,progress:{...progress,bolts:progress.bolts-cost,vehicleProgress:{...progress.vehicleProgress,[vehicleId]:{...build,levels:{...build.levels,[id]:level+1}}}}};}
export function purchasePart(progress,id,vehicleId=progress.selectedVehicle||'cart'){const part=PARTS.find(p=>p.id===id),build=progress.vehicleProgress?.[vehicleId];if(!part||!build)return{ok:false,reason:'Unknown part',progress};if(build.parts[id])return{ok:false,reason:'INSTALLED',progress};const cost=vehiclePartCost(part,vehicleId);if(progress.bolts<cost)return{ok:false,reason:`NEED ${cost-progress.bolts}`,progress};return{ok:true,cost,progress:{...progress,bolts:progress.bolts-cost,vehicleProgress:{...progress.vehicleProgress,[vehicleId]:{...build,parts:{...build.parts,[id]:true}}}}};}
export function purchaseFacility(progress,id){const facility=FACILITY_UPGRADES.find(f=>f.id===id);if(!facility)return{ok:false,reason:'Unknown facility',progress};const level=progress.facilities?.[id]||0;if(level>=facility.max)return{ok:false,reason:'MAXED',progress};const cost=upgradeCost(facility,level);if(progress.bolts<cost)return{ok:false,reason:`NEED ${cost-progress.bolts}`,progress};return{ok:true,cost,progress:{...progress,bolts:progress.bolts-cost,facilities:{...progress.facilities,[id]:level+1}}};}
export function purchasePower(progress,vehicleId=progress.selectedVehicle||'cart'){const system=POWER_SYSTEMS[vehicleId],build=progress.vehicleProgress?.[vehicleId];if(!system||!build)return{ok:false,reason:'Unknown power system',progress};const level=build.powerLevel||0;if(level>=system.max)return{ok:false,reason:'MAXED',progress};const cost=powerCost(system,level);if(progress.bolts<cost)return{ok:false,reason:`NEED ${cost-progress.bolts}`,progress};return{ok:true,cost,progress:{...progress,bolts:progress.bolts-cost,vehicleProgress:{...progress.vehicleProgress,[vehicleId]:{...build,powerLevel:level+1}}}};}
